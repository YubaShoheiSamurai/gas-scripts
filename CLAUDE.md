# CLAUDE.md

## プロジェクト概要

Google Apps Script (GAS) のスクリプト管理リポジトリ。

## Git 運用ルール

**コードを変更するたびに必ず GitHub へプッシュすること。**

具体的な手順:

1. ファイルを変更したら、変更内容を確認する
   ```
   git diff
   git status
   ```
2. 変更ファイルをステージングする
   ```
   git add <変更したファイル>
   ```
3. コミットメッセージは変更内容を端的に表す日本語または英語で記述する
   ```
   git commit -m "変更内容の説明"
   ```
4. **直ちに** GitHub へプッシュする
   ```
   git push origin <ブランチ名>
   ```

### 注意事項

- `git add .` や `git add -A` は使わず、変更ファイルを明示的に指定する
- `.env` や認証情報ファイルは絶対にコミット・プッシュしない
- コミットとプッシュはセットで行い、ローカルのみに変更を残さない

## ディレクトリ構成

```
gas-scripts/
├── CLAUDE.md       # このファイル
└── ...             # スクリプトファイル
```

## 開発方針

- 変更は小さく、頻繁にコミット・プッシュする
- スクリプトは用途ごとにファイルを分けて管理する
- 機密情報（APIキー、パスワードなど）はスクリプト内にハードコードしない
